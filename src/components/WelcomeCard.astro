<article class="welcome-card" aria-label="Welcome card from host">
  <div class="card-topline" aria-hidden="true"></div>

  <header class="card-header">
    <p class="card-kicker">WELCOME CARD</p>
    <img src="/images/icon-sun.svg" alt="" aria-hidden="true" class="card-sun" />
  </header>

  <section class="card-message">
    <p class="card-title-intro">A note from your host,</p>
    <p class="card-title-name">Margaux.</p>
    <p class="card-copy">
      We're so glad you're coming. The shutters will be open, the lemonade cold, and the cat -
      Poivre - pretending not to notice you.
    </p>
  </section>

  <footer class="card-footer">
    <p class="room-kicker">ROOM</p>
    <p class="room-name">La Garrigue</p>
  </footer>
</article>

<style lang="scss">
  .welcome-card {
    position: relative;
    margin-top: 4.5rem;
    padding: 2.8rem 1.5rem;
    border-radius: 1.75rem;
    display: flex;
    flex-direction: column;
    color: var(--neutral-100);
    background:
      radial-gradient(85% 60% at 92% 8%, hsl(22, 72%, 56%) 0%, hsla(22, 72%, 56%, 0) 75%),
      linear-gradient(162deg, hsl(20, 74%, 51%) 0%, hsl(16, 70%, 45%) 42%, hsl(15, 69%, 37%) 100%);
    transform: rotate(2.6deg);
    transform-origin: center;

    // Box shadow using terracotta color to create a subtle glow effect
    box-shadow: 0 0 8px var(--terracotta-500);
  }

  .card-topline {
    height: 1px;
    margin-bottom: 1.75rem;
    background-image: repeating-linear-gradient(
      90deg,
      hsla(35, 60%, 96%, 0.55) 0 9px,
      transparent 9px 16px
    );
    mask-image: linear-gradient(to right, black 0%, hsla(0, 0%, 0%, 0.85) 58%, transparent 100%);
    filter: blur(0.2px);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .card-kicker {
    font-family: var(--font-mono);
    font-size: 1rem;
    letter-spacing: 0.22em;
    color: hsla(35, 60%, 96%, 0.82);
  }

  .card-sun {
    width: 4rem;
    height: 4rem;
    opacity: 0.95;
    margin-right: 0.15rem;
  }

  .card-message {
    margin-top: 3.45rem;
  }

  .card-title-intro {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 6.4vw, 2.4rem);
    font-style: italic;
    line-height: 1.15;
    color: var(--sun-300);
  }

  .card-title-name {
    margin-top: 1.5rem;
    font-family: var(--font-serif);
    font-size: clamp(2rem, 4rem, 5.6rem);
    font-style: italic;
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: 0.01em;
    color: var(--neutral-0);
    word-break: break-all;
  }

  .card-copy {
    margin-top: 2.3rem;
    max-width: 29ch;
    font-family: var(--font-sans);
    font-size: clamp(.85rem, 1.25rem, 1.5rem);
    line-height: 1.5;
    color: var(--neutral-100);
    opacity: 0.8;
  }

  .card-footer {
    margin-top: 6rem;
  }

  .room-kicker {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    letter-spacing: 0.22em;
    color: hsla(35, 60%, 96%, 0.82);
  }

  .room-name {
    margin-top: 1rem;
    font-family: var(--font-serif);
    font-size: clamp(1rem, 2rem, 3.1rem);
    line-height: 1.05;
    color: var(--neutral-0);
    opacity: 0.9;
    font-weight: 400;
  }
</style>
